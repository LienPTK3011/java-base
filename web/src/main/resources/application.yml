server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/java_base
    username: root
    password: root

  flyway:
    enabled: true
    baseline-on-migrate: true               # set true if schema already exists

